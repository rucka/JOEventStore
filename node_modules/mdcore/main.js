"use strict";

function registerGlobals(root) {
    //root._ = root._ || require('underscore');
    //root.Q = root.Q || require('q');
    //root._.str = require('underscore.string');
    //// Mix in non-conflict functions to Underscore namespace if you want
    //root._.mixin(root._.str.exports());
    //// All functions, include conflict, will be available through _.str object
    //root._.str.include('Underscore.string', 'string'); // => true

    //root.__extends = root.__extends || function (d, b) {
    //    function __() { this.constructor = d; }
    //    __.prototype = b.prototype;
    //    d.prototype = new __();
    //};
}

registerGlobals(global);
var requireJs = require('requirejs');

requireJs.config({
    baseUrl: __dirname + '/lib',
    nodeRequire: require
});

module.exports = requireJs('loader');
